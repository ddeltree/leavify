import _ from 'lodash';
import { set, get } from './setter';
import { generateBranches } from './testHelpers';

// const branches = generateBranches();
// set(branches, '0.0.0', 42);
test('aa', () => {
  expect(1).toBe(1);
});
